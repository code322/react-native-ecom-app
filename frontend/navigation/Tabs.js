import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';

const Tabs = () => {
  const TabsStack = createBottomTabNavigator();

  return <TabsStack.Navigator></TabsStack.Navigator>;
};
