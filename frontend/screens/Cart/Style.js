import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  header: {
    fontSize: 20,
    textAlign: 'center',
    fontWeight: 'bold',
  },
  container: {
    marginTop: 20,
  },
});
