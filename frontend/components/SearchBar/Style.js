import { StyleSheet } from 'react-native';
export const style = StyleSheet.create({
  container: {
    flexDirection: 'row',
    width: '100%',
    marginBottom: 20,
    overflow: 'hidden',
  },
  searchIcon: {
    marginRight: 15,
  },
  searchInput: {
    flex: 1,
    borderBottomWidth: 1,
    borderColor: 'black',
    fontSize: 16,
  },
});
