import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  imageContainer: {
    width: '47%',
  },
  image: {
    width: '100%',
    height: 200,
    borderRadius: 10,
    marginBottom: 5,
  },
});
